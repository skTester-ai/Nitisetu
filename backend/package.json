{
  "name": "niti-setu-backend",
  "version": "1.0.0",
  "description": "Voice-Based Agricultural Scheme Eligibility RAG Engine — Backend",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "tunnel": "npx cloudflared tunnel --url http://localhost:3000 --protocol quic --heartbeat-interval 10s --heartbeat-count 2",
    "tunnel:lt": "npx localtunnel --port 3000",
    "tunnel:cf": "npx cloudflared tunnel --url http://localhost:3000 --protocol quic",
    "tunnel:ssh": "ssh -R 80:localhost:3000 serveo.net",
    "tunnel:ping": "ssh -p 443 -R0:localhost:3000 qr@a.pinggy.io",
    "dev:full": "echo. && echo ================================================ && echo [NITI SETU] BOOTING INDUSTRY-GRADE AI GATEWAY && echo ================================================ && echo [TWILIO ACTION]: Copy the HTTPS URL generated by the tunnel && echo [WEBHOOK URL]: Add '/api/whatsapp/webhook' to it. && echo [LOCATION]: Paste in Twilio > Messaging > Sandbox Settings && echo ================================================ && echo. && concurrently -n \"SERVER,BRIDGE\" -c \"blue.bold,green.bold\" \"npm run dev\" \"npm run tunnel\""
  },
  "keywords": [
    "rag",
    "agriculture",
    "eligibility",
    "groq",
    "mongodb",
    "vector-search"
  ],
  "author": "Abhay Patil",
  "license": "MIT",
  "dependencies": {
    "@xenova/transformers": "^2.17.2",
    "apicache": "^1.6.3",
    "axios": "^1.13.6",
    "bcryptjs": "^3.0.3",
    "compression": "^1.8.1",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "express-rate-limit": "^7.5.0",
    "express-validator": "^7.2.1",
    "form-data": "^4.0.5",
    "google-auth-library": "^10.5.0",
    "groq-sdk": "^0.9.1",
    "helmet": "^8.0.0",
    "jsonwebtoken": "^9.0.3",
    "mongoose": "^8.9.5",
    "morgan": "^1.10.0",
    "multer": "^1.4.5-lts.1",
    "neo4j-driver": "^6.0.1",
    "node-fetch": "^3.3.2",
    "nodemailer": "^8.0.1",
    "pdf-parse": "^1.1.0",
    "twilio": "^5.12.2",
    "winston": "^3.17.0"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "nodemon": "^3.1.9"
  }
}
